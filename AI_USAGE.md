# AI 使用说明

## 使用了哪些 AI 工具

- **OpenAI Codex**：作为开发助手，读取项目代码和验收记录，协助分析实现、编辑文档并执行检查。本次协作可确认的使用范围是 README 和本说明的整理，以及 Mock 冒烟和文档差异检查；未核实的历史代码不统一归因为 AI 生成。
- **阿里云百炼模型**：应用运行时使用 Paraformer（`paraformer-realtime-v2`）识别语音、Qwen（`qwen-plus`）生成台词与叙事意图、CosyVoice（`cosyvoice-v3-flash`，配置音色 `longanhuan`）合成语音。这些属于产品能力，区别于开发辅助工具；Mock 模式不调用云模型。

## AI 主要参与了哪些工作

- 梳理本地启动方式、系统模块、语音交互与打断、角色和场景指令、模型与媒体方案。
- 在 Live 对话中生成结构化台词、情绪、动作和剧情事件，由服务端校验和映射后驱动场景；模型输出不直接作为可执行代码运行。

## 哪些关键结果经过了人工验证

已记录的验证包括自动化测试、构建、浏览器自动化检查、真实模型闭环及 30 轮服务采样；这些结果不能替代人工体验确认。

人工验证的关键项是：Mira 音色是否符合角色、对话与演出的自然程度、真实设备上按住说话和打断的体验，以及 Android / iOS 的麦克风权限、音频解锁和横竖屏表现。完成后应补充验证人、日期、设备和结论。

依据：[功能验收记录](docs/acceptance.md)、[语音验收记录](docs/voice-acceptance.md)。

## AI 生成结果存在问题及修正的例子

AI 最初生成的语音打断逻辑只停止了当前正在播放的音频，没有清理待播放队列，也没有过滤旧回合返回的消息。我在试用中连续提问：先让 Mira 介绍照片，在她回答时按住说话追问“你在等谁”。旧声音虽然立即停止，但稍后到达的照片回复又开始播放，字幕也覆盖了新问题的内容。

我检查后发现，问题在于代码把“停止播放器”当成了“取消整个回合”。于是将取消逻辑扩展为：停止音频并清空播放队列，向服务端发送旧回合取消消息；为新输入递增 `turnId`，前端丢弃旧回合消息，服务端在异步任务返回后再次检查回合是否有效。

修正后，我通过模拟旧音频和字幕延迟到达来复测，确认它们不会恢复播放或覆盖新回复。这个例子体现了 AI 可以快速搭出功能，但异步场景下的生命周期和竞态问题仍需要针对性检查。
